; Dynamic 080 dnd

[dynamic-080dnd]
; ${DIPCAST-080DND-AUTO} : 발신자 표시번호 자동 등록 여부 (```1``` or ```0```)
; ${DIPCAST-080DND-INTRO} : 인트로 사운드
; ${DIPCAST-080DND-TEMINATED} : 통화종료 사운드
; ${DIPCAST-080DND-RETRY} : 발신자 표시이름
exten => init,1NoOp(DYNAMIC 080 DND)

 ; Return 서브 루틴 복귀
 same => n(return),Return()